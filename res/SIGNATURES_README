This directory contains the hashes and signatures for PirateWallet-Lite

Verify the hashes by running:
sha256sum -c sha256sum-vX.Y.Z.txt

Verify signatures:
1. First, import the public key (Available on GitHub 
   at https://github.com/mrmlynch/piratewallet-lite/blob/master/public_key.asc)
gpg --import public_key.asc

2. Verify signature 
gpg --verify <filename.sig> <downloaded-filename-to-verify>

